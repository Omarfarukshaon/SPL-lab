#include<stdio.h>
void reverse(int *p, int size){
	int i;
	printf("Reverse order:");
	for(i=size-1;i>=0;i--)
	printf("%2d",*(p+i));
}
int main(){
	int i,size,*p,a[50];
	scanf("%d",&size);
	for(i=0;i<size;i++)
		scanf("%d", &a[i]);
	p=&a[0];
	reverse(p,size);
return 0;
}
