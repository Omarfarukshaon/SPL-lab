#include<stdio.h>
#define PI 3.1416
int main(){
float radius,area,circumference;
scanf("%f",&radius);
area=PI*radius*radius;
circumference=2*PI*radius;
printf("Area and circumference of a circle is,\nArea=%.3f	Circumference=%.3f",area,circumference) ;
return 0;
}
