#include<stdio.h>
int main(){
int i,n,sum=0;
scanf("%d",&n);
int a[n];
printf("Elements of array:");
for(i=0;i<n;i++){
	scanf("%d",&a[i]);
	printf("%d  ",a[i]);
}
printf("\nSum:");
for(i=0;i<n;i++)
	sum+=a[i];
printf("%d",sum);
return 0;
}
