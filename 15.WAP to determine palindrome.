#include<stdio.h>
#include<string.h>
int main(){
char text[20],aux[20];
gets(text);
strcpy(aux,text);
strrev(aux);
if(strcmp(aux,text)==0)
	printf("Palindrome");
else
	printf("Not palindrome");
return 0;
}
